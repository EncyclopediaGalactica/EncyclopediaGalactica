"""
Query Api of the Encyclopedia Galactica system.
The operations below are to retrieve data or system related information.
"""
type Query {
    """
    Returns a list of Document objects.
    """
    getDocuments: [DocumentResult!]!

    """
    Returns the designated object.
    """
    getDocument(id: ID): DocumentResult
}
