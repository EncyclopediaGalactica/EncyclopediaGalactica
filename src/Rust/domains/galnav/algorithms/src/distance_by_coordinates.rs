/// Calculates the distance between two given coordinates in 3D space.
pub async fn calculate_distance_by_coordinates(
    _from_x: &f64,
    _from_y: &f64,
    _from_z: &f64,
    _to_x: &f64,
    _to_y: &f64,
    _to_z: &f64,
) -> anyhow::Result<f64> {
    Ok(0.0)
}
